# Copie para .env.local — Supabase → Project Settings → API
# Use o valor completo do campo "Project URL" (não monte o link na mão).

NEXT_PUBLIC_SUPABASE_URL=https://semewhtxwbmhqayrpbya.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anon_ou_publicavel

# Opcional: Postgres direto (psql, GUI, scripts). Senha em Settings → Database.
# O host direto pode ser só IPv6; em rede só IPv4 use Session Pooler no dashboard.
# DATABASE_URL=postgresql://postgres:SUA_SENHA@db.semewhtxwbmhqayrpbya.supabase.co:5432/postgres

# Nunca commite .env.local com segredos reais.
# Não use service_role no browser.
